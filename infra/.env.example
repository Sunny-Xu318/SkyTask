# Copy this file to `.env` and adjust values if you need to customise the infrastructure stack.

# Nacos authentication (disabled by default)
NACOS_AUTH_ENABLE=false

# Sentinel dashboard login credentials
SENTINEL_USERNAME=sentinel
SENTINEL_PASSWORD=sentinel
